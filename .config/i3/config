#
# i3 config file (v4)
# Please see https://i3wm.org/docs/userguide.html for a complete reference!
#
set $mod Mod4
set $alt Mod1
set $term urxvt
#
font pango:Material Design Icons 8, monospace 8
#
# startup programs
exec --no-startup-id xrdb -merge $XDG_CONFIG_HOME/X11/xresources && $term
exec_always --no-startup-id dunst
exec_always --no-startup-id xautolock -corners +--- -cornerdelay 1 -time 15 -locker "i3lock --color=000000 --ignore-empty-password"
exec_always --no-startup-id redshift -c $XDG_CONFIG_HOME/redshift/redshift.conf
#
# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod
#
#bindsym $mod+d exec dmenu_run
bindsym $mod+d exec "rofi -modi window,drun,combi -show combi -sidebar-mode"
bindsym $mod+F4 kill
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
#
bindsym $mod+Return exec $term
#
# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right
#
# move focused window
bindsym $mod+Control+Shift+h move left
bindsym $mod+Control+Shift+j move down
bindsym $mod+Cintrol+Shift+k move up
bindsym $mod+Control+Shift+l move right
bindsym $mod+Control+Shift+Left move left
bindsym $mod+Control+Shift+Down move down
bindsym $mod+Control+Shift+Up move up
bindsym $mod+Control+Shift+Right move right

# split window
bindsym $mod+Shift+Right split h; exec $term
bindsym $mod+Shift+Down split v; exec $term

# switch workspace
bindsym $mod+Tab exec rofi -show window
bindsym $mod+period workspace next
bindsym $mod+comma workspace prev

# enter fullscreen mode for the focused container
bindsym $mod+$alt+f fullscreen toggle

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Floating apps
for_window [class="Ardour"] move container to workspace $ws6, workspace $ws6, floating enable, focus
for_window [class="Audacity"] move container to workspace $ws6, workspace $ws6, floating enable, focus
for_window [class="Blender"] move container to workspace $ws5, workspace $ws5, floating enable, focus
for_window [class="Code"] move container to workspace $ws3, workspace $ws3, floating enable, focus
for_window [class="Firefox"] move container to workspace $ws2, workspace $ws2, floating enable, focus
for_window [class="Gimp"] move container to workspace $ws7, workspace $ws7, floating enable, focus
for_window [class="google-chrome"] move container to workspace $ws2, workspace $ws2, floating enable, focus
for_window [class="Google-chrome"] move container to workspace $ws2, workspace $ws2, floating enable, focus
for_window [title="htop"] move container to workspace $ws10, workspace $ws10, floating enable, focus
for_window [class="Inkscape"] move container to workspace $ws7, workspace $ws7, floating enable, focus
for_window [class="KDEnlive"] move container to workspace $ws5, workspace $ws5, floating enable, focus
for_window [title="mc"] move container to workspace $ws10, workspace $ws10, floating enable, focus
for_window [class="Openshot"] move container to workspace $ws5, workspace $ws5, floating enable, focus
for_window [class="Pitivi"] move container to workspace $ws5, workspace $ws5, floating enable, focus
for_window [class="Shotcut"] move container to workspace $ws5, workspace $ws5, floating enable, focus
for_window [class="Tor Browser"] move container to workspace $ws2, workspace $ws2, floating enable, focus
for_window [class="vim"] move container to workspace $ws3, workspace $ws3, floating enable, focus
for_window [class="vlc"] move container to workspace $ws5, workspace $ws5, floating enable, focus
for_window [class="Wireshark"] move container to workspace $ws10, workspace $ws10, floating enable, focus

set $ws1 "1: Terminal"
set $ws2 "2: Web"
set $ws3 "3: Code"
set $ws4 "4: "
set $ws5 "5: Video"
set $ws6 "6: Audio"
set $ws7 "7: Graphics"
set $ws8 "8: "
set $ws9 "9: "
set $ws10 "10: Tools"
# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Set shut down, restart and locking features
bindsym $mod+Escape mode "$mode_system"
set $mode_system (l)ock, (e)xit, (s)uspend, (h)ibernate, (Shift+r)eboot, (Shift+s)hutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id i3lock --color=000000 --ignore-empty-password, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id "i3lock && systemctl suspend", mode "default"
    bindsym h exec --no-startup-id "i3lock && systemctl hibernate", mode "default"
    bindsym Shift+r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Shift+s exec --no-startup-id systemctl poweroff, mode "default"

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

bar {
        status_command py3status --config ~/.config/i3/status.conf
        workspace_buttons yes
        position bottom
        mode dock
}
